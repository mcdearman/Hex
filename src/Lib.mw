-- hex: bytes as hexadecimal text and back. A port of Rust's `hex` crate,
-- version 0.4.3.
--
--   use Hex (encodeString, decode)
--
--   encodeString "kiwi"  -- "6b697769"
--   decode "6b697769"    -- Ok #[107, 105, 119, 105]

mod Tables
mod Hex
@cfg(test)
mod Cases
@cfg(test)
mod Tests

@pub use Hex.Hex
